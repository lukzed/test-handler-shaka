{
  "name": "test-handler-shaka",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "start": "pnpm build && node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
